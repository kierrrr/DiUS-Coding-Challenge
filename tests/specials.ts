// import {Chec} from "../interfaces"

// const threeForTwo = (state: CheckoutState, sku: string) => {
//     const skus = state.cart.filter(element => element.sku == sku);

//     const newCart = skus.reduce((prev, curr) => {


//         prev.count += 1;
//     }, { newCart: new Array<StoreStock>(), count: 0 })
// }

// export default {
//     threeForTwo
// }
